*Добавить в модели UserEntity поле для хранения кэша рефреш токена.
*В методе generateTokens записывать в базу для пользвателя хэш рефреш токена.
*Создать методы setRefreshTokenHash и getRefreshTokenHash для хранения хэша рефреш токена в базе, а также для удаления removeRefreshTokenHash.
*В методе refreshToken достать хэш рефреш токена из базы, расшифровать и сделать compare с переданным refresh токеном.
*Если хэш рефреш токена не совпадает, то возвращать ошибку, чистить хэш токена у пользователя в базе.
*Если хэш рефреш токена совпадает, то возвращать новый токен и обновить хэш токена у пользователя в базе.
*При логауте очищаем хэш токена у пользователя в базе.